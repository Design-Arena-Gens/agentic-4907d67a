# روبوت رد رسائل إنستغرام

واجهة مبنية بـ **Next.js 14** تساعدك على تصميم سيناريوهات رد تلقائي لرسائل إنستغرام الخاصة، مع محاكاة مباشرة ونموذج تكامل جاهز بلغة Node.js.

## ✨ المزايا

- مُركّب بصري للكلمات المفتاحية والردود مع تصنيف ذكي للرسائل.
- اختيار فوري لنبرة الحديث (ودّية / سريعة / ترويجية) يساعد على ضبط شخصية الروبوت.
- معاينة حيّة لصندوق الرسائل توضح شكل الردود قبل الإطلاق.
- مقتطف كود Node.js جاهز للدمج مع Webhook الخاص بواجهات Meta.
- قائمة مرجعية لأهم خطوات التكامل الأمني مع منصة Meta.
- بطاقات مؤشرات أداء لمنحك لمحة عن حالة الحملة التشغيلية.

## 🚀 التشغيل محليًا

```bash
npm install
npm run dev
```

ثم افتح [http://localhost:3000](http://localhost:3000).

## 🧱 بنية المشروع

```
.
├── app/
│   ├── layout.tsx      # التهيئة العامة والخط
│   ├── page.tsx        # الواجهة الرئيسية والوظائف التفاعلية
│   └── globals.css     # الأنماط العالمية
├── public/
│   └── icon.svg        # أيقونة التطبيق
├── next.config.mjs
├── package.json
└── tsconfig.json
```

## 🛠️ سكريبتات npm

- `npm run dev` لتشغيل الخادم التطويري.
- `npm run build` لإنشاء نسخة الإنتاج.
- `npm run start` لتشغيل نسخة الإنتاج بعد البناء.
- `npm run lint` لفحص الجودة وفق إعدادات Next.js.

## 🔌 التكامل مع Instagram Graph API

1. فعّل حساب مطوّر في منصة Meta واربط حساب إنستغرام التجاري.
2. أنشئ Webhook يتحقق من التوقيع (`X-Hub-Signature-256`) قبل معالجة الرسائل.
3. أخزن القواعد المولّدة في قاعدة بيانات قابلة للتحديث دون إعادة النشر.
4. استخدم المقتطف الموجود داخل الواجهة كنقطة انطلاق لخدمة الردود.

## 📄 الترخيص

الكود متاح للاستخدام والتعديل بحرية ضمن شروط رخصة MIT الافتراضية. يمكنك تخصيص الواجهة وإضافة تكاملات خلفية بما يتناسب مع احتياجات مشروعك.
